// Temporary function for testing - use your personal email
export function rollNumberToEmailTest(rollNo: string): string {
  // Replace with YOUR email for testing
  return "your.email@gmail.com"
  
  // Original function (restore this later):
  // const cleaned = rollNumber.replace("-", "")
  // const year = cleaned.substring(0, 2)
  // const section = cleaned.substring(2, 3)
  // const number = cleaned.substring(3)
  // return `${section}${year}${number}@cfd.nu.edu.pk`.toLowerCase()
}

